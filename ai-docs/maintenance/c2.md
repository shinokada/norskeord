# C2 after migration

## Fine as-is

Keep the following as they are:

- `blog.test.ts` — the `cefrLevels(['C2'])` test is testing the function with arbitrary input, not a level constant — leave it
- `quiz.test.ts` — the comment `// Only 1 other C2 entry` is in test fixture code that deliberately uses `level: 'C'` already — the comment is stale but harmless; the test already works correctly
- `searchUtils.test.ts` — `{ level: 'C2' }` in the filter test is testing "filter returns empty when no match" — it's testing the search engine's filter logic with a non-existent level value. That's actually a valid test of the "no results" path. Leave it.
- `stats/+page.svelte` — already fully migrated to `'C'` (the regex `\b(A1|A2|B1|B2|C1|C2)\b` in `buildShareText` is for parsing the estimate string which already won't contain C1/C2 anymore — harmless but could be cleaned up)
